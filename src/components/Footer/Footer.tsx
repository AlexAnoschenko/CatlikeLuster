import { Layout } from 'antd';
import { FC } from 'react';

const Footer: FC = () => {
  return (
    <Layout.Footer style={{ background: 'gray' }}>
      Footer
    </Layout.Footer>
  );
};

export default Footer;
